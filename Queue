package test;
import java.util.ArrayList;
import java.util.List;
import java.util.Queue;
import java.util.Iterator;
import java.util.PriorityQueue;


					/*
.                    The PriorityQueue class implements the Queue interface. It holds the elements or objects which are to be 
processed by their priorities. PriorityQueue doesn't allow null values to be stored in the queue
					 */	  
      class container<T>
      {
    	  T b;
    	  
      }
public class demo extends Thread {
	
	public static void main(String[] args)   throws Exception
	{
		Queue<String> q1 = new PriorityQueue(); 
		q1.add("tmu");
		q1.add("jnu");
		q1.add("du");
		q1.add("lpu");
		q1.add("iit");
		System.out.println(q1.element()+" : Head Element  ");
		System.out.println(q1.peek()+ " : Peek Element");
		
		
		Iterator<String> ii = q1.iterator();
		while(ii.hasNext())
		{
			System.out.println(ii.next());
		}
		q1.remove();  
		q1.poll();  
		System.out.println("after removing two elements:");  
		Iterator<String> itr2=q1.iterator();  
		while(itr2.hasNext()){  
		System.out.println(itr2.next());  
		}  
		
		
}	
}
  






		


